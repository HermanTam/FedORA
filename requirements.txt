# Preferred setup uses conda. Create the base environment with:
#   conda env create -f environment.yml
# Then activate it:
#   conda activate feddaa
# The packages listed below are the Python-level extras that remain the same
# across CUDA- and CPU-only installs. Install them via:
#   pip install -r requirements.txt

#numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5
scikit-image>=0.24
matplotlib>=3.9
tqdm>=4.66
tensorboard>=2.18
#opencv-python==4.7.*  # it’s handled by environment.yml
wand>=0.6.10
pyyaml>=6.0
requests>=2.32
networkx>=3.2
cvxpy>=1.6
